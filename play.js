console.log("Hello");

//npm init -y (Installs node modules and saves as a node file)
//npm install//

//pwd= (print working directory)

//npm i express (installs the express dependecies in node)

//inside your new folder/make sure you are at top level!
// insert a new folder named src/standard term/practice
//create a .gitignore file= enter

//node_modules
// .env
// .vscode
// .DS_Store

//use commit statements that are professional

//to create a branch git checkout -b express-static //to check your in correct branch type git branch

// import express from "express" an older way to import in data
//use const functioname = require ("functioname")
//use clear node src/server.js to check server is running

//A server handles and recieves requests from the client

//When a server gets a request it will locate and sort the info in a database
//The server then sends the client back a response+
